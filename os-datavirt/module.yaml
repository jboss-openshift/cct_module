schema_version: 1
name: os-datavirt
version: '1.0'
description: Legacy os-datavirt script package.

modules:
  install:
  - name: os-eap-migration

execute:
- script: configure.sh
  user: '185'

packages:
      repositories:
          - jboss-os
      install:
          - hostname

run:
      user: 185
      cmd:
          - "/opt/eap/bin/openshift-launch.sh"
