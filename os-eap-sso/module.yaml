schema_version: 1
name: os-eap-sso
version: '1.0'
description: Legacy os-eap-sso script package.
execute:
- script: configure.sh
  user: '185'
packages:
      repositories:
          - jboss-os
      install:
          - hostname
