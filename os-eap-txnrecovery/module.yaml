schema_version: 1
name: os-eap-txnrecovery
version: '1.0'
description: Transaction recovery migration pod script package.
execute:
- script: install_as_root.sh
packages:
    repositories:
        - jboss-os
    install:
        - python-enum34
        - python-requests
