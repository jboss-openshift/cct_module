schema_version: 1
name: os-eap-launch
version: '1.0'
description: Legacy os-eap-launch script package.
execute:
- script: configure.sh
  user: '185'
packages:
      repositories:
          - jboss-os
      install:
          - hostname
