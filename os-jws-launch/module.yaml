schema_version: 1
name: os-jws-launch
version: '1.0'
description: Legacy os-jws-launch script package.
execute:
- script: run
  user: '185'
envs:
    - name: "ENABLE_ACCESS_LOG"
      example: "true"
      description: Enable the Access Log.
