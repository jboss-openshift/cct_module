schema_version: 1
name: os-eap64-launch
version: '1.0'
description: Legacy os-eap64-launch script package.

modules:
  install:
  - name: os-eap-migration

execute:
- script: configure.sh
  user: '185'
